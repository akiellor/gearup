#!/usr/bin/env ruby

require 'rubygems'
require 'rack'
require 'less'
require 'coffee-script'
require 'sprockets'


env = Sprockets::Environment.new ARGV[0] || "."
env.append_path "."

Rack::Handler::WEBrick.run(env, :Port => 3000)

