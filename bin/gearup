#!/usr/bin/env ruby

require 'rubygems'
require 'thin'
require 'less'
require 'coffee-script'
require 'sprockets'

Thin::Server.start('0.0.0.0', 3000) do
  env = Sprockets::Environment.new ARGV[0] || "."
  env.append_path "."
  run env
end
